{
  "name": "pareto-tslint-rules",
  "version": "0.0.4",
  "description": "tslint rules to enforce the pareto style of coding",
  "main": "dist/index.js",
  "scripts": {
    "build": "rd /s /q .\\dist && tsc",
    "lint": "tslint --project tsconfig.json",
    "pubMin": "git diff --exit-code && rd /s /q .\\dist && tsc && npm run lint && npm run test && npm version minor && git push && npm publish",
    "pubPatch": "git diff --exit-code && rd /s /q .\\dist && tsc && npm run lint && npm run test && npm version patch && git push && npm publish",
    "test": "tslint --test ./test/rules/pareto",
    "u2l": "echo 'don't update to latest, A specific version of typescript is needed for tslint'"
  },
  "keywords": [],
  "author": "corno",
  "license": "ISC",
  "types": "dist/index.d.ts",
  "prepublish": "tsc",
  "repository": {
    "type": "git",
    "url": "https://github.com/corno/pareto-tslint-rules.git"
  },
  "peerDependencies": {
    "typescript": ">=2.4.0-dev || >=2.5.0-dev || >=2.6.0-dev || >=2.7.0-dev || >=2.8.0-dev || >=2.9.0-dev || >=3.0.0-dev || >= 3.1.0-dev || >= 3.2.0-dev",
    "tslint": ">=5.0.0"
  },
  "dependencies": {
    "@types/node": "^12.12.14",
    "fountain-pen": "^1.0.1"
  },
  "devDependencies": {
    "tslint": "5.20.0",
    "typescript": "3.7.2"
  }
}
