// tslint:disable: no-shadowed-variable
import { RootBuilder } from "../../src/builder"

export function createRoot(rb: RootBuilder) {
    const bla: string = "FOO"
    print(`foo ${true} bar`)
    ~~~~~~~~~~~~~~~~~~~~~~~~  [expression statements should not be used]
                 ~~~~         [unsupported expression kind: TrueKeyword]
    throw new Error("SDFS")
    ~~~~~~~~~~~~~~~~~~~~~~~  [unsupported statement kind: ThrowStatement]
}
